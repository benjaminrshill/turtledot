import React from 'react';

export default function Clear(props) {

    return (
        <main>
            <button>
                clear items
            </button>
            <button>
                clear weeks
            </button>
            <button>
                clear all
            </button>
        </main>
    );
}
